pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' } // ← برای کتابخانه تقویم شمسی
    }
}

dependencyResolutionManagement {
    // 🔄 از PREFER_SETTINGS به PREFER_PROJECT تغییر داده شد
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "hesab"
include(":app")
