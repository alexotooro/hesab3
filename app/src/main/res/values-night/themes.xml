<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌙 تم شب برای حالت تیره برنامه حساب -->
    <style name="Theme.Hesab" parent="Theme.AppCompat.NoActionBar">
        <!-- رنگ‌های اصلی تیره‌تر -->
        <item name="colorPrimary">@color/toolbar_blue_dark</item>
        <item name="colorPrimaryDark">@color/toolbar_blue_dark</item>
        <item name="colorAccent">@color/add_button_blue</item>

        <!-- رنگ متن و پس‌زمینه -->
        <item name="android:windowBackground">@color/app_background_dark</item>
        <item name="android:textColor">@color/table_text_dark</item>

        <!-- فونت -->
        <item name="android:fontFamily">@font/iran_sans</item>

        <!-- رنگ استاتوس‌بار و نوار پایین -->
        <item name="android:statusBarColor">@color/toolbar_blue_dark</item>
        <item name="android:navigationBarColor">@color/toolbar_blue_dark</item>

        <!-- دکمه‌ها و فیلدهای ورودی -->
        <item name="android:buttonStyle">@style/Widget.Button.Custom.Night</item>
        <item name="android:editTextStyle">@style/Widget.EditText.Custom.Night</item>
    </style>

    <!-- 💠 استایل دکمه‌ها در حالت شب -->
    <style name="Widget.Button.Custom.Night" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/add_button_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/iran_sans</item>
    </style>

    <!-- ✏️ استایل فیلد متنی در حالت شب -->
    <style name="Widget.EditText.Custom.Night" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/gray</item>
        <item name="android:fontFamily">@font/iran_sans</item>
    </style>

</resources>
