// 📄 MainActivity.kt
package org.hesab.app

import android.content.Intent
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import androidx.recyclerview.widget.LinearLayoutManager
import androidx.recyclerview.widget.RecyclerView
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.launch
import org.hesab.app.data.AppDatabase
import org.hesab.app.data.Transaction
import android.widget.*

class MainActivity : AppCompatActivity() {

    private lateinit var recyclerView: RecyclerView
    private lateinit var adapter: TransactionAdapter
    private lateinit var db: AppDatabase
    private lateinit var btnAddTransaction: Button
    private lateinit var btnSettings: Button
    private lateinit var chkOnlyExpense: CheckBox
    private lateinit var chkOnlyIncome: CheckBox
    private lateinit var tvBalance: TextView

    private var allTransactions: MutableList<Transaction> = mutableListOf()

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        db = AppDatabase.getDatabase(this)
        recyclerView = findViewById(R.id.recyclerView)
        btnAddTransaction = findViewById(R.id.btnAddTransaction)
        btnSettings = findViewById(R.id.btnSettings)
        chkOnlyExpense = findViewById(R.id.chkOnlyExpense)
        chkOnlyIncome = findViewById(R.id.chkOnlyIncome)
        tvBalance = findViewById(R.id.tvBalance)

        recyclerView.layoutManager = LinearLayoutManager(this)

        adapter = TransactionAdapter(
            onEdit = { transaction ->
                val intent = Intent(this, AddTransactionActivity::class.java)
                intent.putExtra("transaction_id", transaction.id)
                startActivity(intent)
            },
            onDelete = { transaction ->
                CoroutineScope(Dispatchers.IO).launch {
                    db.transactionDao().delete(transaction)
                    loadTransactions()
                }
            }
        )

        recyclerView.adapter = adapter

        btnAddTransaction.setOnClickListener {
            startActivity(Intent(this, AddTransactionActivity::class.java))
        }

        btnSettings.setOnClickListener {
            startActivity(Intent(this, SettingsActivity::class.java))
        }

        chkOnlyExpense.setOnCheckedChangeListener { _, _ -> applyFilters() }
        chkOnlyIncome.setOnCheckedChangeListener { _, _ -> applyFilters() }
    }

    override fun onResume() {
        super.onResume()
        loadTransactions()
    }

    private fun loadTransactions() {
        CoroutineScope(Dispatchers.IO).launch {
            val transactions = db.transactionDao().getAll()
            allTransactions = transactions.toMutableList()
            runOnUiThread {
                adapter.submitList(allTransactions)
                updateBalance(allTransactions)
            }
        }
    }

    private fun applyFilters() {
        var filtered = allTransactions

        if (chkOnlyExpense.isChecked) {
            filtered = filtered.filter { it.type == "expense" }.toMutableList()
        }

        if (chkOnlyIncome.isChecked) {
            filtered = filtered.filter { it.type == "income" }.toMutableList()
        }

        adapter.submitList(filtered)
        updateBalance(filtered)
    }

    private fun updateBalance(list: List<Transaction>) {
        val balance = list.sumOf {
            if (it.type == "income") it.amount else -it.amount
        }
        tvBalance.text = "مانده: %,d ریال".format(balance)
    }
}
